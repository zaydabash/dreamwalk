# Configuration for scripts/train_decoder.py
# Any key here overrides the script's command-line defaults.

samples: 10000
channels: 8
seed: 42
device: auto

models:
  - eeg_to_clip
  - emotion_classifier
  - motif_detector

output_dir: models/checkpoints
